// Example: Display a message on project click
document.querySelectorAll('.project-card').forEach(card => {
    card.addEventListener('click', () => {
      alert('You clicked on a project!');
    });
  });
  